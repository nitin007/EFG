<div class="page-header">
  <h1>Sign In</h1>
</div>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="alert alert-warning" style="color: black;">
    <h2>System-wide password reset</h2>
    <p>
      Following a security vulnerability in TLS (the mechanism which allows https websites), we have taken the proactive measure of requiring all users to reset their password. There is no evidence that any data has been compromised from this website, but sensible security policies mean that we should assume the worst and take appropriate measures.
    </p>
  </div>

  <%= f.input :username, required: false, autofocus: true %>
  <%= f.input :password, required: false %>

  <%= render 'devise/shared/links' %>
  <%= link_to 'Forgot your username?', new_usernames_reminder_path %>

  <div class="form-actions">
    <%= f.button :submit, 'Sign In', class: 'btn btn-primary' %>
  </div>
<% end %>
